{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "DocuMind is a PUBLIC repo; RootDispatcher (the private-repo preset source) cannot be resolved here — it previously halted with \"Cannot find preset's package\" (issue #16). Extends the public DESIGN-DVW/renovate-config mirror instead of inlining the policy by hand (see DISPATCH-094).",
  "extends": ["github>DESIGN-DVW/renovate-config"]
}
