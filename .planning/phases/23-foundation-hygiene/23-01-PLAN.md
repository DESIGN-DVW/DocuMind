---
phase: 23-foundation-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - docs/slides/external/2026-05-21-figma-ai-pitch-deck.html
  - docs/slides/external/2026-05-21-figma-ai-pitch-deck.pdf
  - docs/slides/external/2026-05-21-figma-ai-pitch-deck.pptx
  - docs/slides/internal/2026-05-21-figma-ai-internal-deck.html
  - docs/slides/internal/2026-05-21-figma-ai-internal-deck.pdf
  - docs/slides/internal/2026-05-21-figma-ai-internal-deck.pptx
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "git ls-files shows no .html/.pdf/.pptx files under docs/slides/"
    - "All 6 previously-tracked slide export files still exist on disk"
    - "The two .md deck sources remain tracked in git"
    - "Newly rendered slide exports under docs/slides/ are ignored by git"
    - "JSDoc HTML (docs/07-api/jsdoc/) and dashboard HTML (dashboard/*.html) remain tracked and NOT ignored"
  artifacts:
    - path: ".gitignore"
      provides: "Path-scoped ignore rules for docs/slides/**/*.{html,pdf,pptx}"
      contains: "docs/slides/**/*.html"
  key_links:
    - from: ".gitignore"
      to: "docs/slides/"
      via: "path-scoped glob patterns (not repo-wide extension bans)"
      pattern: "docs/slides/\\*\\*/\\*\\.(html|pdf|pptx)"
---

<objective>
Remove the 6 stale committed slide export binaries (HTML/PDF/PPTX) from the git index without deleting the local copies, and add narrowly-scoped .gitignore rules so rendered exports never get committed again. No history rewrite (explicitly out of scope per REQUIREMENTS.md).

Purpose: v3.4 decision — EN Marp .md decks in docs/slides/ are the ONLY hand-edited slide artifact. All rendered exports are regenerated on demand (Phase 24) and must not live in git.
Output: Clean git index (6 export files untracked, .md sources still tracked), .gitignore with path-scoped exclusions.

Note on branching: this phase touches config files (.gitignore) and starts a new milestone. Per the global Branching Protocol, execution should happen on a fresh branch cut from `master` (e.g. `feat/2026-07-10-v3.4-foundation-hygiene`), NOT on the currently checked-out `fix/2026-07-07-table-lint-rules` branch. The two deck .md files show unrelated local modifications ("Buzz-cleaned" content edits per STATE.md) — do not commit or revert those as part of this plan.
</objective>

<execution_context>
@/Users/dave/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23-foundation-hygiene/23-RESEARCH.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add path-scoped .gitignore rules and untrack the 6 slide export files</name>
  <files>.gitignore</files>
  <action>
1. Append a new section to `.gitignore` (matching the file's existing style of narrow, path-scoped exclusions like `data/kuzu-smoke-test/`):

```gitignore
# Presentation pipeline: rendered exports are regenerated on demand, never committed
docs/slides/**/*.html
docs/slides/**/*.pdf
docs/slides/**/*.pptx
```

Do NOT use repo-wide `*.html` / `*.pdf` patterns — the repo has tracked HTML that must stay tracked (docs/07-api/jsdoc/*.html JSDoc output, dashboard/diagrams.html, dashboard/obsolete.html).

1. Untrack exactly these 6 files with per-file `git rm --cached` invocations (explicit paths, NOT a directory-level or glob removal — `docs/slides/**/*.md` sources must stay tracked):

```bash
git rm --cached docs/slides/external/2026-05-21-figma-ai-pitch-deck.html
git rm --cached docs/slides/external/2026-05-21-figma-ai-pitch-deck.pdf
git rm --cached docs/slides/external/2026-05-21-figma-ai-pitch-deck.pptx
git rm --cached docs/slides/internal/2026-05-21-figma-ai-internal-deck.html
git rm --cached docs/slides/internal/2026-05-21-figma-ai-internal-deck.pdf
git rm --cached docs/slides/internal/2026-05-21-figma-ai-internal-deck.pptx
```

CRITICAL: the `--cached` flag must be present on every invocation — without it, git deletes the files from disk, violating FOUND-01 ("remain present on disk locally"). Do NOT touch the sibling `.md` files.
  </action>
  <verify>
    <automated>git ls-files docs/slides | grep -E '\.(html|pdf|pptx)$' | wc -l  # must output 0
ls docs/slides/external/2026-05-21-figma-ai-pitch-deck.html docs/slides/external/2026-05-21-figma-ai-pitch-deck.pdf docs/slides/external/2026-05-21-figma-ai-pitch-deck.pptx docs/slides/internal/2026-05-21-figma-ai-internal-deck.html docs/slides/internal/2026-05-21-figma-ai-internal-deck.pdf docs/slides/internal/2026-05-21-figma-ai-internal-deck.pptx  # all 6 must exist on disk</automated>
  </verify>
  <done>`git ls-files` shows zero .html/.pdf/.pptx entries under docs/slides/; all 6 files still present on disk; git status shows the 6 files as staged deletions (index only) plus a modified .gitignore.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no collateral damage to other tracked files</name>
  <files>(none — verification only)</files>
  <action>
Run a negative-scope sweep to confirm the new ignore rules and index surgery hit ONLY the intended files:

1. Deck sources still tracked: `git ls-files docs/slides | grep '\.md$'` must list both `docs/slides/external/2026-05-21-figma-ai-pitch-deck.md` and `docs/slides/internal/2026-05-21-figma-ai-internal-deck.md`.
2. Other HTML unaffected: `git check-ignore -v dashboard/diagrams.html dashboard/obsolete.html` must return nothing (exit 1 = not ignored). Spot-check one JSDoc file: `git check-ignore -v $(git ls-files 'docs/07-api/jsdoc/*.html' | head -1)` must also return nothing (skip if no jsdoc html tracked).
3. New exports ARE ignored going forward: `git check-ignore docs/slides/internal/2026-05-21-figma-ai-internal-deck.html` must succeed (exit 0), confirming the untracked copies won't reappear in `git status`.

If check 2 shows any dashboard/jsdoc file as ignored, the .gitignore pattern is too broad — fix the pattern before committing (this is Pitfall 2 from research).
  </action>
  <verify>
    <automated>git ls-files docs/slides | grep -c '\.md$'  # must output 2
git check-ignore dashboard/diagrams.html; test $? -eq 1  # not ignored
git check-ignore docs/slides/internal/2026-05-21-figma-ai-internal-deck.html  # exit 0 = ignored</automated>
  </verify>
  <done>Both .md deck sources tracked; dashboard + JSDoc HTML not ignored; slide exports ignored. FOUND-01 success criterion fully satisfied.</done>
</task>

</tasks>

<verification>
- `git ls-files | grep -iE 'docs/slides.*\.(html|pdf|pptx)$'` returns empty
- All 6 export files exist on disk (no data loss)
- `git ls-files docs/slides` still lists exactly the 2 `.md` sources
- `git check-ignore -v dashboard/diagrams.html` returns nothing (pattern is path-scoped, not repo-wide)
- No history rewrite performed (no filter-repo/BFG; only `git rm --cached` + .gitignore)
</verification>

<success_criteria>
FOUND-01 satisfied: rendered slide exports absent from the git index, present on disk, future exports gitignored via docs/slides/-scoped patterns, .md sources untouched and tracked.
</success_criteria>

<output>
After completion, create `.planning/phases/23-foundation-hygiene/23-01-SUMMARY.md`
</output>
