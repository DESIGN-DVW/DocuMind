---
phase: 05-mcp-server-write-tools
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .mcp.json
autonomous: true
requirements:
  - MCPW-06

must_haves:
  truths:
    - "All 16 DVWDesign repos have a documind entry in their .mcp.json"
    - "Existing MCP server entries in each repo's .mcp.json are preserved (not overwritten)"
    - "Per-repo DIAGRAM-REGISTRY.md files in RootDispatcher and any2figma are reverse-synced to DB then deleted"
  artifacts:
    - path: ".mcp.json (in each of 16 repos)"
      provides: "DocuMind MCP server registration"
      contains: "documind"
  key_links:
    - from: "each repo .mcp.json"
      to: "daemon/mcp-server.mjs"
      via: "absolute path in args field"
      pattern: "DocuMind/daemon/mcp-server.mjs"
---

<objective>
Register the DocuMind MCP server in all 16 DVWDesign repos' `.mcp.json` files and centralize the diagram registry by reverse-syncing then deleting per-repo DIAGRAM-REGISTRY.md files.

Purpose: Make DocuMind tools discoverable by Claude Code agents in every DVWDesign repo, and establish DocuMind DB as the single source of truth for diagrams.
Output: 16 updated `.mcp.json` files, 2 deleted per-repo DIAGRAM-REGISTRY.md files.
</objective>

<execution_context>
@/Users/dave/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-server-write-tools/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register DocuMind MCP server in all 16 repos' .mcp.json files</name>
  <files>.mcp.json (across 16 repos)</files>
  <action>
Write a Node.js script (inline via bash, or a temporary script) that reads each repo's `.mcp.json`, merges in the `documind` server entry, and writes it back. The script must preserve all existing MCP server entries.

The `documind` entry to merge into each `.mcp.json`:
```json
{
  "mcpServers": {
    "documind": {
      "command": "node",
      "args": ["/Users/Shared/htdocs/github/DVWDesign/DocuMind/daemon/mcp-server.mjs"],
      "env": {
        "DOCUMIND_DB": "/Users/Shared/htdocs/github/DVWDesign/DocuMind/data/documind.db",
        "DOCUMIND_PROFILE": "/Users/Shared/htdocs/github/DVWDesign/DocuMind/config/profiles/dvwdesign.json"
      }
    }
  }
}
```

**All 16 repo paths (absolute):**
1. `/Users/Shared/htdocs/github/DVWDesign/DocuMind/.mcp.json`
2. `/Users/Shared/htdocs/github/DVWDesign/RootDispatcher/.mcp.json`
3. `/Users/Shared/htdocs/github/DVWDesign/FigmaAPI/@figma-core/.mcp.json`
4. `/Users/Shared/htdocs/github/DVWDesign/FigmaAPI/FigmaDSController/.mcp.json`
5. `/Users/Shared/htdocs/github/DVWDesign/FigmaAPI/@figma-docs/.mcp.json`
6. `/Users/Shared/htdocs/github/DVWDesign/FigmaAPI/FigmailAPP/.mcp.json`
7. `/Users/Shared/htdocs/github/DVWDesign/CampaignManager/.mcp.json`
8. `/Users/Shared/htdocs/github/DVWDesign/AdobePlugIns/.mcp.json`
9. `/Users/Shared/htdocs/github/DVWDesign/shared-packages/.mcp.json`
10. `/Users/Shared/htdocs/github/DVWDesign/Figma-Plug-ins/.mcp.json`
11. `/Users/Shared/htdocs/github/DVWDesign/Aprimo/.mcp.json`
12. `/Users/Shared/htdocs/github/DVWDesign/any2figma/.mcp.json`
13. `/Users/Shared/htdocs/github/DVWDesign/mjml-dev-mode/.mcp.json`
14. `/Users/Shared/htdocs/github/DVWDesign/LibraryAssetManager/.mcp.json`
15. `/Users/Shared/htdocs/github/DVWDesign/RandD/.mcp.json`
16. `/Users/Shared/htdocs/github/DVWDesign/GlossiaApp/.mcp.json`

**Merge logic for each file:**
1. Read existing `.mcp.json` — if it fails to parse or doesn't exist, start with `{ "mcpServers": {} }`
2. Ensure `mcpServers` key exists: `existing.mcpServers = existing.mcpServers || {}`
3. Set `existing.mcpServers.documind = { command, args, env }` (overwrite any existing documind entry — safe since we own it)
4. Write back with `JSON.stringify(existing, null, 2) + '\n'`

**Important:** DocuMind's own `.mcp.json` (repo #1 above) currently only has `figma-desktop`. The `documind` entry exists in `.claude/mcp.json` (Phase 4). Adding it to `.mcp.json` too is correct — `.mcp.json` is the standard location for repo-level MCP config.

After the script runs, verify all 16 files contain the `documind` key.
  </action>
  <verify>
    <automated>for repo in DocuMind RootDispatcher "FigmaAPI/@figma-core" FigmaAPI/FigmaDSController "FigmaAPI/@figma-docs" FigmaAPI/FigmailAPP CampaignManager AdobePlugIns shared-packages Figma-Plug-ins Aprimo any2figma mjml-dev-mode LibraryAssetManager RandD GlossiaApp; do f="/Users/Shared/htdocs/github/DVWDesign/$repo/.mcp.json"; if node -e "const j=JSON.parse(require('fs').readFileSync('$f','utf-8')); if(!j.mcpServers?.documind) process.exit(1)" 2>/dev/null; then echo "OK: $repo"; else echo "FAIL: $repo"; fi; done</automated>
  </verify>
  <done>All 16 DVWDesign repos have `documind` in their `.mcp.json` with correct absolute paths. Existing MCP server entries in each file are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Reverse-sync and delete per-repo DIAGRAM-REGISTRY.md files</name>
  <files>Per-repo DIAGRAM-REGISTRY.md files (RootDispatcher, any2figma)</files>
  <action>
Centralize the diagram registry by reverse-syncing the two per-repo DIAGRAM-REGISTRY.md files into the DocuMind DB, then deleting them.

**Step 1: Reverse-sync** — Run reverse-sync for both repos that have per-repo registry files:
```javascript
import { reverseSyncFromRegistry } from '../processors/relink-processor.mjs';
// For RootDispatcher:
await reverseSyncFromRegistry(db, 'RootDispatcher', '/Users/Shared/htdocs/github/DVWDesign/RootDispatcher');
// For any2figma:
await reverseSyncFromRegistry(db, 'any2figma', '/Users/Shared/htdocs/github/DVWDesign/any2figma');
```

This can be done via a small inline Node.js script that opens the DB read-write, imports the function, calls it for both repos, then closes the DB.

**Step 2: Delete** — Remove the per-repo DIAGRAM-REGISTRY.md files:
- `/Users/Shared/htdocs/github/DVWDesign/RootDispatcher/docs/diagrams/DIAGRAM-REGISTRY.md` (verify path first — may be at a different location within the repo)
- `/Users/Shared/htdocs/github/DVWDesign/any2figma/docs/diagrams/DIAGRAM-REGISTRY.md` (same)

Use `find` or `glob` to locate the exact file paths within each repo before deleting. Only delete files named `DIAGRAM-REGISTRY.md` in these two repos.

**Step 3: Verify** — Confirm the files no longer exist and the diagrams table in the DB has data from the reverse-sync.

**Important:** Do NOT delete DocuMind's own `docs/diagrams/DIAGRAM-REGISTRY.md` — that is the consolidated snapshot generated by `curate_diagram`.
  </action>
  <verify>
    <automated>echo "--- Checking per-repo registries deleted ---"; for repo in RootDispatcher any2figma; do found=$(find "/Users/Shared/htdocs/github/DVWDesign/$repo" -name "DIAGRAM-REGISTRY.md" 2>/dev/null | head -1); if [ -z "$found" ]; then echo "OK: $repo registry deleted"; else echo "FAIL: $repo still has $found"; fi; done</automated>
  </verify>
  <done>Per-repo DIAGRAM-REGISTRY.md files in RootDispatcher and any2figma have been reverse-synced to the DocuMind DB and deleted from disk. DocuMind DB diagrams table is the single source of truth for all diagram data.</done>
</task>

</tasks>

<verification>
1. `node -e "..." | grep -c OK` on all 16 repos returns 16 (all registered)
2. No DIAGRAM-REGISTRY.md files exist in RootDispatcher or any2figma
3. DocuMind DB `diagrams` table has entries (can verify via `sqlite3 data/documind.db "SELECT COUNT(*) FROM diagrams"`)
</verification>

<success_criteria>
- All 16 DVWDesign repos have `documind` MCP server in `.mcp.json`
- Existing MCP server entries preserved in every repo
- Per-repo DIAGRAM-REGISTRY.md files reverse-synced and deleted
- DocuMind DB is the single source of truth for diagram data
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-server-write-tools/05-02-SUMMARY.md`
</output>
