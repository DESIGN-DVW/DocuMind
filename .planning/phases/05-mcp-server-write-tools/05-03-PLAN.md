---
phase: 05-mcp-server-write-tools
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - MCPW-01
  - MCPW-02
  - MCPW-03
  - MCPW-04
  - MCPW-05
  - MCPW-06

must_haves:
  truths:
    - "All 11 tools listed cleanly in MCP Inspector without parse errors"
    - "A write tool call (lint_file) returns structured JSON with issues"
    - "A path outside repo roots returns an error response, not a crash"
  artifacts: []
  key_links: []
---

<objective>
Verify all 11 MCP tools (6 read + 5 write) work correctly via MCP Inspector. Confirm no stdout pollution, structured JSON responses, and path validation.

Purpose: Human verification that the MCP server works end-to-end before marking Phase 5 complete.
Output: Verified MCP server with 11 tools.
</objective>

<execution_context>
@/Users/dave/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify MCP server tools in MCP Inspector</name>
  <files>daemon/mcp-server.mjs</files>
  <action>
Human verification of MCP server functionality via MCP Inspector. No code changes — this is a verification-only checkpoint.
  </action>
  <verify>
    <automated>cd /Users/Shared/htdocs/github/DVWDesign/DocuMind && grep -c 'server.tool' daemon/mcp-server.mjs | xargs -I{} test {} -ge 11 && echo '11 tools registered' || echo 'FAIL'</automated>
  </verify>
  <done>Human confirms all 11 tools work in MCP Inspector without errors.</done>
  <what-built>11 MCP tools (6 read + 5 write) in daemon/mcp-server.mjs with path validation and read-write DB</what-built>
  <how-to-verify>
1. Run MCP Inspector:
   ```bash
   cd /Users/Shared/htdocs/github/DVWDesign/DocuMind
   npx @modelcontextprotocol/inspector node daemon/mcp-server.mjs
   ```

2. Verify tool listing: All 11 tools should appear:
   - search_docs, get_related, get_keywords, get_tree, check_existing, get_diagrams
   - index_file, lint_file, fix_file, trigger_scan, curate_diagram

3. Test `lint_file` with a known file:
   - Call with `{ "file": "/Users/Shared/htdocs/github/DVWDesign/DocuMind/CLAUDE.md" }`
   - Should return JSON with `success`, `issues` array, `total`, `fixable_count`

4. Test path validation:
   - Call `lint_file` with `{ "file": "/etc/passwd" }`
   - Should return `isError: true` with "Path rejected" message

5. Test `trigger_scan`:
   - Call with `{ "mode": "incremental" }` (or `{ "mode": "incremental", "repo": "DocuMind" }`)
   - Should return scan results with `added`, `updated`, `skipped` counts

6. Verify no stdout pollution: No garbled JSON or unexpected text in the Inspector console
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 11 tools work in MCP Inspector.
</verification>

<success_criteria>
- MCP Inspector connects and lists all 11 tools
- lint_file returns structured issue list
- Path validation rejects /etc/passwd
- trigger_scan completes and returns results
- No stdout pollution visible
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-server-write-tools/05-03-SUMMARY.md`
</output>
