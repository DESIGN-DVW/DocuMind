---
phase: 16-kuzu-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/kuzu-smoke-test.mjs
autonomous: true
requirements:
  - GRAPH-03
must_haves:
  truths:
    - "Running `node scripts/kuzu-smoke-test.mjs` exits 0 and prints 'Kuzu smoke test PASSED'"
    - "The kuzu package is installed at version 0.11.3 and appears in package.json dependencies"
    - "The ESM import form that works is documented in a comment at the top of kuzu-smoke-test.mjs"
    - "`docker build -t documind-test .` completes without source-compilation messages for kuzu"
    - "`docker run --rm documind-test node scripts/kuzu-smoke-test.mjs` exits 0"
  artifacts:
    - path: "scripts/kuzu-smoke-test.mjs"
      provides: "Empirical ESM import verification + DB open/close lifecycle check"
    - path: "package.json"
      provides: "kuzu@0.11.3 in dependencies"
  key_links:
    - from: "scripts/kuzu-smoke-test.mjs"
      to: "kuzu npm package"
      via: "ESM import (default or createRequire fallback)"
      pattern: "import kuzu from 'kuzu'|createRequire.*kuzu"
---

<objective>
Install kuzu@0.11.3 and empirically verify it loads in Node.js 22 ESM before writing any application code. Run a smoke test locally and inside Docker to resolve the highest-risk item in Phase 16.

Purpose: The ESM import compatibility of kuzu in .mjs files is unverified. The smoke test must confirm which import form works (default import or createRequire fallback) before server.mjs is touched. This plan gates all subsequent Phase 16 work.

Output: `scripts/kuzu-smoke-test.mjs` (throwaway verification script), kuzu@0.11.3 in package.json, Docker build + run confirmed clean.
</objective>

<execution_context>
@/Users/dave/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-kuzu-foundation/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install kuzu@0.11.3 and write smoke test script</name>
  <files>package.json, scripts/kuzu-smoke-test.mjs</files>
  <action>
Run `npm install kuzu@0.11.3` from the DocuMind project root. This installs the package and updates package.json.

Then create `scripts/kuzu-smoke-test.mjs` with the following logic:

1. Attempt `import kuzu from 'kuzu'` (ESM default import). If this form is used, add a comment at the top: `// ESM import: default import works — use throughout Phase 16`.

2. If the default import throws `ERR_PACKAGE_PATH_NOT_EXPORTED` or `SyntaxError: The requested module 'kuzu' does not provide an export named 'default'`, switch to the createRequire fallback:
   ```javascript
   import { createRequire } from 'module';
   const require = createRequire(import.meta.url);
   const kuzu = require('kuzu');
   ```
   Add a comment: `// ESM import: createRequire fallback required — use throughout Phase 16`.

3. The test body must:
   - Resolve a temp directory at `path.join(__dirname, '../data/kuzu-smoke-test')` where `__dirname = path.dirname(fileURLToPath(import.meta.url))`
   - Delete the temp dir if it exists (`fs.rmSync(testDir, { recursive: true })`)
   - Open a database: `const db = new kuzu.Database(testDir)`
   - Open a connection: `const conn = new kuzu.Connection(db)`
   - Run: `await conn.query('CREATE NODE TABLE SmokeTest(id INT64, PRIMARY KEY(id))')`
   - Run: `await conn.query("CREATE (:SmokeTest {id: 1})")`
   - Run and verify: `const result = await conn.query('MATCH (n:SmokeTest) RETURN n.id'); const rows = await result.getAll(); if (!rows.length) throw new Error('No rows returned');`
   - Log: `console.log('Kuzu smoke test PASSED. Rows:', rows);`
   - Verify `.close()` methods: call `conn.close()` — if it throws, log a warning but continue; call `db.close()` — same handling.
   - Delete the temp dir: `fs.rmSync(testDir, { recursive: true })`

4. Add a comment near the `.close()` calls documenting which methods exist (for server.mjs shutdown implementation in Plan 03).

Add `"smoke-test": "node scripts/kuzu-smoke-test.mjs"` to the `scripts` section of package.json.

Also add `data/kuzu-smoke-test/` and `data/documind.kuzu/` to `.gitignore` if not already present.
  </action>
  <verify>
    <automated>cd /Users/Shared/htdocs/github/DVWDesign/DocuMind && node scripts/kuzu-smoke-test.mjs</automated>
  </verify>
  <done>Script exits 0 and prints "Kuzu smoke test PASSED". The import form used is documented in a comment at the top of the file. `.close()` behavior is documented in a comment.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Docker build and runtime with kuzu installed</name>
  <files>(no new files — verifies existing Dockerfile)</files>
  <action>
Run the following commands in sequence. The Dockerfile already uses `node:22-bookworm-slim` with `python3 make g++` in the builder stage — no Dockerfile changes are needed.

```bash
docker build -t documind-test .
docker run --rm documind-test node scripts/kuzu-smoke-test.mjs
```

If the Docker build fails with a kuzu source-compilation error (gcc/make output for kuzu), that indicates the pre-built binary was not used. In that case: check if `node_modules/kuzu/` contains a `.node` prebuilt file. If kuzu tries to compile from source, add `ENV npm_config_build_from_source=false` to the Dockerfile BEFORE the `npm ci` step (builder stage).

If the Docker run fails with the same ESM error as potentially seen in Task 1, ensure the same import form (createRequire fallback if needed) is used consistently.

The expected output inside Docker is identical to local: "Kuzu smoke test PASSED."

After both commands succeed, note in a comment at the bottom of `scripts/kuzu-smoke-test.mjs`:
```javascript
// Docker verified: node:22-bookworm-slim + kuzu@0.11.3 confirmed working
// Dockerfile changes required: [none | add ENV npm_config_build_from_source=false]
```
  </action>
  <verify>
    <automated>docker build -t documind-test /Users/Shared/htdocs/github/DVWDesign/DocuMind && docker run --rm documind-test node scripts/kuzu-smoke-test.mjs</automated>
  </verify>
  <done>Docker build exits 0 without kuzu source-compilation messages. `docker run` exits 0 and prints "Kuzu smoke test PASSED". kuzu-smoke-test.mjs has a comment confirming Docker verification status and any Dockerfile changes made.</done>
</task>

</tasks>

<verification>
- `node scripts/kuzu-smoke-test.mjs` exits 0, prints "Kuzu smoke test PASSED"
- `cat package.json | grep kuzu` shows `"kuzu": "0.11.3"`
- `docker build -t documind-test . && docker run --rm documind-test node scripts/kuzu-smoke-test.mjs` exits 0
- scripts/kuzu-smoke-test.mjs contains a comment documenting which ESM import form works
- scripts/kuzu-smoke-test.mjs contains a comment documenting `.close()` method availability
</verification>

<success_criteria>
1. `import kuzu from 'kuzu'` or the createRequire fallback is confirmed working in Node.js 22 ESM — smoke test exits 0
2. Docker image builds with Kuzu pre-built binaries (no source compilation) and passes the smoke test inside the container
3. The working import form is documented — Plans 02 and 03 can use it without re-investigation
</success_criteria>

<output>
After completion, create `.planning/phases/16-kuzu-foundation/16-01-SUMMARY.md` following the summary template.

CRITICAL: Include in the summary:
- Which ESM import form works: `import kuzu from 'kuzu'` (default) OR `createRequire` fallback
- Whether `conn.close()` and `db.close()` methods exist
- Whether any Dockerfile change was needed
These facts are load-bearing for Plan 02 and Plan 03.
</output>
