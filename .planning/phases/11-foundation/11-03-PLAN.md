---
phase: 11-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - scripts/scan-all-repos.mjs
  - scripts/fix-markdown.mjs
  - scripts/fix-custom-errors.mjs
  - scripts/watch-and-index.mjs
  - scripts/propagate-lint-rules.mjs
  - scripts/propagate-org-fixes.mjs
  - scripts/fix-github-org-references.mjs
  - scripts/scan/enhanced-scanner.mjs
  - CLAUDE.md
autonomous: true
requirements:
  - FNDTN-01
  - FNDTN-02
  - FNDTN-04

must_haves:
  truths:
    - "All scripts in scripts/ resolve BASE_PATH/basePath from config/constants.mjs LOCAL_BASE_PATH, not hardcoded strings"
    - "scripts/scan/enhanced-scanner.mjs builds repo paths from LOCAL_BASE_PATH + repo names, not 8 inline paths"
    - "CLAUDE.md documents the env var configuration system and notes machine-specific paths"
    - "Running grep -r '/Users/Shared' daemon/ processors/ config/constants.mjs scripts/ returns only the single fallback in constants.mjs"
  artifacts:
    - path: "scripts/scan-all-repos.mjs"
      provides: "Scanner using LOCAL_BASE_PATH from constants"
      contains: "import.*constants"
    - path: "scripts/scan/enhanced-scanner.mjs"
      provides: "Enhanced scanner using LOCAL_BASE_PATH + repo names"
      contains: "import.*constants"
    - path: "CLAUDE.md"
      provides: "Updated documentation reflecting env var config"
      contains: "DOCUMIND_REPOS_DIR"
  key_links:
    - from: "scripts/scan-all-repos.mjs"
      to: "config/constants.mjs"
      via: "import { LOCAL_BASE_PATH }"
      pattern: "import.*LOCAL_BASE_PATH.*constants"
    - from: "scripts/scan/enhanced-scanner.mjs"
      to: "config/constants.mjs"
      via: "import { LOCAL_BASE_PATH }"
      pattern: "import.*LOCAL_BASE_PATH.*constants"
---

<objective>
Replace all hardcoded macOS paths in scripts/ and update CLAUDE.md to document the new env var configuration system.

Purpose: Scripts are the remaining source of hardcoded paths after Plans 01-02 handle daemon/processors. CLAUDE.md must reflect the env var system because agents read it as their primary reference. This completes the FNDTN-01 audit scope defined in CONTEXT.md.

Output: 8 updated scripts importing LOCAL_BASE_PATH from config/constants.mjs, updated CLAUDE.md with env var documentation
</objective>

<execution_context>
@/Users/dave/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundation/11-CONTEXT.md
@.planning/phases/11-foundation/11-RESEARCH.md
@.planning/phases/11-foundation/11-01-SUMMARY.md
@config/constants.mjs
@CLAUDE.md

<interfaces>
<!-- From config/constants.mjs (updated by Plan 01) -->
From config/constants.mjs:
```javascript
export const LOCAL_BASE_PATH;  // REPOS_DIR env var or '/Users/Shared/htdocs/github/DVWDesign'
export const ACTIVE_REPOSITORIES;
export const REPO_PATH_MAP;
export function getLocalPath(repoName);
export function getAllRepositoryNames();
```

<!-- From config/env.mjs (created by Plan 01) -->
From config/env.mjs:
```javascript
export const REPOS_DIR;   // string | null
export const REPOS_LIST;  // string[] | null
export const PORT;
export const DB_PATH;
// ... all other exports
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded paths in all scripts/</name>
  <files>scripts/scan-all-repos.mjs, scripts/fix-markdown.mjs, scripts/fix-custom-errors.mjs, scripts/watch-and-index.mjs, scripts/propagate-lint-rules.mjs, scripts/propagate-org-fixes.mjs, scripts/fix-github-org-references.mjs, scripts/scan/enhanced-scanner.mjs</files>
  <action>
For each of these 8 script files, apply the same pattern:

1. Find the hardcoded BASE_PATH / basePath declaration (typically near the top in a CONFIG object or as a standalone const)
2. Replace with an import from config/constants.mjs:
   ```javascript
   import { LOCAL_BASE_PATH } from '../config/constants.mjs';
   ```
   (or `'../../config/constants.mjs'` for scripts/scan/enhanced-scanner.mjs which is one level deeper)
3. Replace the hardcoded value with `LOCAL_BASE_PATH`
4. If the script uses `BASE_PATH` as the variable name, either rename to `LOCAL_BASE_PATH` throughout or add `const BASE_PATH = LOCAL_BASE_PATH;` for minimal diff

**scripts/scan/enhanced-scanner.mjs** is special — it has 8 individual hardcoded repo paths. Replace all of them:
- Each path like `'/Users/Shared/htdocs/github/DVWDesign/DocuMind'` becomes `path.join(LOCAL_BASE_PATH, 'DocuMind')` (or use the existing repo name strings with `LOCAL_BASE_PATH` prefix)
- Add `import path from 'path';` if not already imported

Do NOT change any script logic, only the path resolution. Each script must continue to work identically when LOCAL_BASE_PATH resolves to the same value as the old hardcoded path.
  </action>
  <verify>
    <automated>grep -rn "'/Users/Shared" scripts/ --include='*.mjs' | grep -v node_modules | wc -l | xargs -I{} test {} -eq 0 && echo "OK: zero hardcoded paths in scripts/" || (echo "FAIL: hardcoded paths remain" && grep -rn "'/Users/Shared" scripts/ --include='*.mjs' && exit 1)</automated>
  </verify>
  <done>All 8 scripts import LOCAL_BASE_PATH from config/constants.mjs. Zero hardcoded '/Users/Shared' strings remain in scripts/.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with env var configuration documentation</name>
  <files>CLAUDE.md</files>
  <action>
Update CLAUDE.md to reflect the env var configuration system. Per CONTEXT.md: "Also update: CLAUDE.md (agents read it, must reflect env var config)".

1. In the "Absolute path" line near the top (line ~30), add a note: the path is the local macOS default; set `DOCUMIND_REPOS_DIR` to override for other environments.

2. Add a new section "## Environment Configuration" after the "Daemon Mode" section, containing:
   - Brief explanation: "DocuMind loads configuration from environment variables, with optional `.env` file support via Node 22's `process.loadEnvFile()`. All config is centralized in `config/env.mjs`."
   - Table of all env vars with name, default, and description (PORT, DOCUMIND_DB, DOCUMIND_PROFILE, DOCUMIND_REPOS_DIR, DOCUMIND_REPOS, DOCUMIND_CRON_HEARTBEAT through CRON_RELINK)
   - Note: "Copy `.env.example` to `.env` for local development. In Docker, pass vars directly. The daemon starts without `.env` — macOS defaults are used as fallbacks."

3. In the RootDispatcher Integration section (line ~490), note that the memory file path uses the local macOS default and resolves from DOCUMIND_REPOS_DIR when set.

Keep changes minimal and focused. Do NOT rewrite unrelated sections. Follow existing CLAUDE.md formatting conventions (ATX headings, table format with `| --- |` separators).
  </action>
  <verify>
    <automated>grep -c 'DOCUMIND_REPOS_DIR' CLAUDE.md | xargs -I{} test {} -ge 2 && echo "OK: CLAUDE.md documents DOCUMIND_REPOS_DIR" || (echo "FAIL: CLAUDE.md missing env var docs" && exit 1)</automated>
  </verify>
  <done>CLAUDE.md documents the env var configuration system including all DOCUMIND_* variables. Machine-specific paths are annotated as defaults. Agents reading CLAUDE.md understand how to configure DocuMind for non-macOS environments.</done>
</task>

</tasks>

<verification>
1. `grep -rn "'/Users/Shared" scripts/ daemon/ processors/ --include='*.mjs' | grep -v node_modules` — should return ZERO results
2. `grep -c "'/Users/Shared" config/constants.mjs` — should return exactly 1 (the fallback)
3. `grep 'DOCUMIND_REPOS_DIR' CLAUDE.md` — should return at least 2 matches (env var section + absolute path note)
4. `grep 'Environment Configuration' CLAUDE.md` — new section exists
5. `node -e "import('./scripts/scan-all-repos.mjs').catch(e => console.log('Import check:', e.message?.slice(0,80)))"` — script module loads without syntax errors
</verification>

<success_criteria>
- `grep -r '/Users/Shared' daemon/ processors/ scripts/ --include='*.mjs'` returns zero results
- Only one hardcoded macOS path remains in the entire production codebase: the fallback in config/constants.mjs
- CLAUDE.md has a new "Environment Configuration" section documenting all env vars
- All scripts continue to work identically on the local macOS machine
</success_criteria>

<output>
After completion, create `.planning/phases/11-foundation/11-03-SUMMARY.md`
</output>
