---
phase: 04-mcp-server-read-tools
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - .claude/mcp.json
autonomous: false
requirements: [MCPR-08]

must_haves:
  truths:
  - "Claude Code agents across DVWDesign repos can discover and call DocuMind MCP tools"
  - "MCP Inspector connects cleanly and lists all 6 tools without parse errors"
  - "search_docs returns results when called from Inspector"
  artifacts:
  - path: ".claude/mcp.json"
      provides: "Claude Code MCP server registration"
      contains: "documind"
  key_links:
  - from: ".claude/mcp.json"
      to: "daemon/mcp-server.mjs"
      via: "command + args config"
      pattern: "mcp-server\\.mjs"
---

<objective>
Register DocuMind MCP server in Claude Code's project-level config and verify all tools work end-to-end via MCP Inspector.

Purpose: Without registration, Claude Code agents cannot discover or call DocuMind tools. This plan completes the stdio integration (MCPR-08) and provides human verification that the full Phase 4 works correctly.

Output: `.claude/mcp.json` with documind server entry, verified tool connectivity
</objective>

<execution_context>
@/Users/dave/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-mcp-server-read-tools/04-01-SUMMARY.md
@.planning/phases/04-mcp-server-read-tools/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register MCP server in Claude Code project config</name>
  <files>.claude/mcp.json</files>
  <action>
    Check if `.claude/mcp.json` exists. If it does, read it and add the documind entry. If not, create it.

    The file should contain (merge with existing if present):
    ```json
    {
      "mcpServers": {
        "documind": {
          "command": "node",
          "args": ["/Users/Shared/htdocs/github/DVWDesign/DocuMind/daemon/mcp-server.mjs"],
          "env": {
            "DOCUMIND_DB": "/Users/Shared/htdocs/github/DVWDesign/DocuMind/data/documind.db",
            "DOCUMIND_PROFILE": "/Users/Shared/htdocs/github/DVWDesign/DocuMind/config/profiles/dvwdesign.json"
          }
        }
      }
    }
    ```

    Use absolute paths — Claude Code resolves from the project root, and this MCP server may be called from other DVWDesign repos that have different working directories.
  </action>
  <verify>cat .claude/mcp.json | node -e "const j=JSON.parse(require('fs').readFileSync(0,'utf8')); console.log(j.mcpServers.documind ? 'OK' : 'MISSING')"</verify>
  <done>.claude/mcp.json exists with documind server entry pointing to absolute path of daemon/mcp-server.mjs</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete MCP server with 6 read tools (search_docs, get_related, get_keywords, get_tree, check_existing, get_diagrams), PM2 config, and Claude Code registration</what-built>
  <how-to-verify>
    1. Run `npm run mcp:inspect` (or `npx @modelcontextprotocol/inspector node daemon/mcp-server.mjs`)
    2. Verify the Inspector connects without parse errors (no stdout pollution)
    3. Confirm all 6 tools appear in the tool list
    4. Call `search_docs` with query "sqlite" — should return ranked results
    5. Call `get_related` with a doc_id from the search results — should return relationship graph
    6. Call `get_diagrams` with no filters — should return diagram list
    7. Verify stderr shows `[mcp-server] DocuMind MCP ready` (not stdout)
  </how-to-verify>
  <resume-signal>Type "approved" if all tools work, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `.claude/mcp.json` exists with valid JSON and documind server entry
2. MCP Inspector connects without JSON parse errors
3. All 6 tools listed in Inspector
4. search_docs returns results for a known query
5. No stdout pollution visible in Inspector output
</verification>

<success_criteria>
- .claude/mcp.json has documind entry with absolute paths
- MCP Inspector shows clean connection with 6 tools
- At least one tool call returns valid results from the live database
- User has approved the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/04-mcp-server-read-tools/04-02-SUMMARY.md`
</output>
