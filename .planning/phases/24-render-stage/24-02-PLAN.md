---
phase: 24-render-stage
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified: []
autonomous: false
requirements: [RNDR-03]

must_haves:
  truths:
    - "Invoking the render path server-side under PM2's process environment succeeds — Chrome binary resolution is verified daemon-side, not just in an interactive shell"
    - "The verification does NOT rely on `pm2 env <id>` (which does not reflect .env-loaded vars in this codebase); it is functional — an ephemeral PM2 process that actually runs the render and whose logs/exit are inspected"
    - "The editable-PPTX success path is explicitly flagged as environment-blocked (LibreOffice not installed) and marked human_needed, not silently skipped"
  artifacts:
    - path: .planning/phases/24-render-stage/24-02-SUMMARY.md
      provides: "Captured PM2 render evidence (log excerpt, exit status, produced output files) + environment-blocked notes"
      min_lines: 15
  key_links:
    - from: "ephemeral pm2 process (documind-render-test)"
      to: scripts/publish-slides.mjs
      via: "pm2 start node --no-autorestart -- scripts/publish-slides.mjs --render-only <fixture>"
      pattern: "pm2 start node"
    - from: "PM2 process environment"
      to: "puppeteer Chromium resolution"
      via: "renderDeck() succeeds under PM2 (HOME-driven executablePath resolves identically to interactive shell)"
      pattern: "puppeteer\\.executablePath"
---

<objective>
Prove RNDR-03: the render path (built in Plan 24-01) succeeds when invoked server-side under the PM2 daemon environment, not just an interactive shell. Chrome binary resolution must be verified daemon-side.

Purpose: PM2's non-interactive process environment can resolve binaries differently from an interactive shell. Research confirmed HOME (which drives `puppeteer.executablePath()`) is identical between PM2 and the shell on this machine, which de-risks browser resolution — but RNDR-03 requires a functional daemon-side proof, not an assumption.

CRITICAL corrected method (from 24-RESEARCH.md Pitfall 2): do NOT verify with `pm2 env <id>`. It does not reflect `config/env.mjs`'s runtime-loaded `.env` vars for this codebase (confirmed live: SOFFICE_PATH/DEEPL_API_KEY/FTP_HOST are absent from `pm2 jlist`'s snapshot despite being exported). Verify functionally with an ephemeral `pm2 start ... --no-autorestart` process that actually runs the render and whose logs/exit code are inspected. Do NOT add these vars to ecosystem.config.cjs's `env:` block to "fix" the snapshot — that duplicates the source of truth config/env.mjs already owns.

Output: `.planning/phases/24-render-stage/24-02-SUMMARY.md` with the captured evidence. No source files change in this plan.
</objective>

<execution_context>
@/Users/dave/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-render-stage/24-RESEARCH.md
@.planning/phases/24-render-stage/24-01-PLAN.md

# Precedent for flagging environment-blocked verification as human_needed:
@.planning/phases/23-foundation-hygiene/23-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the ephemeral-PM2 render verification and capture evidence</name>
  <files>.planning/phases/24-render-stage/24-02-SUMMARY.md</files>
  <action>
Verify the render succeeds under PM2's process environment using the research-corrected functional method (NOT `pm2 env`).

1. Confirm the daemon is healthy after a restart (roadmap criterion 3 references `pm2 restart documind`):
   ```bash
   pm2 restart documind && pm2 show documind | grep -E 'status|uptime'
   curl -s localhost:9000/health | head
   ```
   (The `documind` daemon itself exposes no render trigger until Phase 29, so the render proof uses a dedicated ephemeral process below — this step only confirms the restart didn't break the daemon.)

2. Spawn a throwaway, non-persistent PM2 process that actually runs the render code against ONE fixture (keeps runtime bounded), then inspect its logs and exit:
   ```bash
   pm2 start node --no-autorestart --name documind-render-test \
     -- scripts/publish-slides.mjs --render-only \
     docs/slides/external/2026-05-21-figma-ai-pitch-deck.md
   # give it time to finish (headless render is multi-second per format), then:
   pm2 logs documind-render-test --lines 200 --nostream
   pm2 jlist | node -e "let d=JSON.parse(require('fs').readFileSync(0));let p=d.find(x=>x.name==='documind-render-test');console.log('status:',p?.pm2_env?.status,'exit:',p?.pm2_env?.exit_code)"
   ```

3. Confirm the three output files were (re)produced by the PM2-run render (check mtimes are fresh):
   ```bash
   ls -la docs/slides/external/2026-05-21-figma-ai-pitch-deck.{html,pdf,pptx}
   ```

4. Clean up the ephemeral process:
   ```bash
   pm2 delete documind-render-test
   ```

5. Write `.planning/phases/24-render-stage/24-02-SUMMARY.md` capturing: the log excerpt showing render success under PM2, the ephemeral process exit status (0), the produced output file listing with fresh timestamps, and an explicit note that Chrome resolution succeeded daemon-side (proving RNDR-03's browser half). Note that the SOFFICE_PATH warn branch fired as expected (LibreOffice absent) — this is the RNDR-02 non-editable path, correct behavior under PM2. Follow markdown table rules (minimal `| - |` separators, no blank lines between rows).
  </action>
  <verify>
    <automated>PDF=docs/slides/external/2026-05-21-figma-ai-pitch-deck.pdf; START_TS=$(date +%s); pm2 start node --no-autorestart --name documind-render-test -- scripts/publish-slides.mjs --render-only docs/slides/external/2026-05-21-figma-ai-pitch-deck.md; for i in $(seq 1 60); do ST=$(pm2 jlist | node -e "let d=JSON.parse(require('fs').readFileSync(0));let p=d.find(x=>x.name==='documind-render-test');console.log(p?.pm2_env?.status||'gone')"); if [ "$ST" = "stopped" ] || [ "$ST" = "errored" ] || [ "$ST" = "gone" ]; then break; fi; sleep 10; done; STATUS=$(pm2 jlist | node -e "let d=JSON.parse(require('fs').readFileSync(0));let p=d.find(x=>x.name==='documind-render-test');console.log(p?.pm2_env?.exit_code)"); pm2 delete documind-render-test; [ -f "$PDF" ] && [ "$(stat -f %m "$PDF")" -gt "$START_TS" ] && echo "exit_code=$STATUS fresh=yes" || { echo "PDF missing or stale (mtime not newer than START_TS)"; exit 1; }</automated>
  </verify>
  <done>An ephemeral PM2 process ran `scripts/publish-slides.mjs` and exited 0; its logs show the render succeeded (Chrome resolved daemon-side); the .html/.pdf/.pptx outputs were reproduced with fresh timestamps; the SOFFICE_PATH warn branch fired as expected; evidence is captured in 24-02-SUMMARY.md. `pm2 env` was NOT used.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm RNDR-03 daemon-side render and accept editable-PPTX env-block</name>
  <files>.planning/phases/24-render-stage/24-02-SUMMARY.md</files>
  <action>Human reviews the captured PM2 render evidence (see &lt;how-to-verify&gt;) and confirms RNDR-03 is verified daemon-side, then acknowledges the editable-PPTX success path is environment-blocked (LibreOffice absent) and accepts it as human_needed per the Phase 23 precedent. No code is written in this task — it gates on human confirmation of Task 1's evidence.</action>
  <what-built>
Functional RNDR-03 verification: an ephemeral PM2 process (`documind-render-test`) ran the render path server-side and produced HTML/PDF/PPTX, proving Chrome binary resolution works under PM2's environment (not just an interactive shell). The `documind` daemon was restarted and confirmed healthy. `pm2 env` was deliberately avoided per the research-corrected method.
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/24-render-stage/24-02-SUMMARY.md` — confirm the PM2 log excerpt shows a successful render and exit code 0.
2. Confirm the three output files at `docs/slides/external/2026-05-21-figma-ai-pitch-deck.{html,pdf,pptx}` have fresh timestamps from the PM2 run.
3. Acknowledge the ENVIRONMENT-BLOCKED item: the RNDR-02 editable-PPTX SUCCESS path (real slide objects via `--pptx-editable`) cannot be exercised on this machine because LibreOffice is not installed (`/Applications/LibreOffice.app` absent). The warn/non-editable branch IS verified. Per the Phase 23 precedent (Docker-lockfile criterion), this editable-success path is marked `human_needed` / environment-blocked — NOT a phase blocker. To unblock later: `brew install --cask libreoffice`, then re-run `npm run slides:build` and confirm the returned `pptxEditable:true`.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm RNDR-03 verified daemon-side and the editable-PPTX gap accepted as environment-blocked, or describe issues.</resume-signal>
  <verify>Human reads 24-02-SUMMARY.md and confirms the PM2 log excerpt shows exit code 0 + a successful render, and the three output files carry fresh timestamps.</verify>
  <done>Human types "approved": RNDR-03 is confirmed verified under PM2 daemon-side, and the editable-PPTX success path is accepted as environment-blocked (human_needed).</done>
</task>

</tasks>

<verification>
- Ephemeral PM2 render process exits 0 and its logs show a successful multi-format render.
- The render outputs are (re)produced with fresh timestamps by the PM2-invoked run — Chrome resolves daemon-side (RNDR-03 browser half proven).
- `pm2 env <id>` is NOT used anywhere in the verification (research-corrected method honored).
- The editable-PPTX success path is explicitly flagged environment-blocked/human_needed (LibreOffice absent), mirroring Phase 23's Docker precedent — not counted as a failure.
</verification>

<success_criteria>
- RNDR-03: renders succeed under the PM2 daemon environment, verified functionally (ephemeral PM2 process), with browser resolution confirmed daemon-side.
- The one unverifiable sub-claim (editable PPTX) is honestly flagged as environment-blocked rather than asserted.
</success_criteria>

<output>
After completion, `.planning/phases/24-render-stage/24-02-SUMMARY.md` exists with the PM2 render evidence and the environment-blocked note. This plan changes no source files.
</output>
