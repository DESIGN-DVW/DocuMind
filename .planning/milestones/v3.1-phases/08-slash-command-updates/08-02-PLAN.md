---
phase: 08-slash-command-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/figma-curate.md
  - /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/memory/global-rules.md
autonomous: true
requirements: [SLSH-03, SLSH-04]

must_haves:
  truths:
    - "/figma-curate uses curate_diagram MCP tool for relinking instead of manual file editing and curl calls"
    - "global-rules.md declares the diagrams table as single source of truth and DIAGRAM-REGISTRY.md as a generated snapshot"
  artifacts:
    - path: "/Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/figma-curate.md"
      provides: "Rewritten slash command using curate_diagram MCP tool"
      contains: "curate_diagram"
    - path: "/Users/Shared/htdocs/github/DVWDesign/RootDispatcher/memory/global-rules.md"
      provides: "Updated diagram section declaring DB as source of truth"
      contains: "single source of truth"
  key_links:
    - from: "figma-curate.md"
      to: "curate_diagram MCP tool (Tool 13)"
      via: "MCP tool call replaces manual file edit + curl"
      pattern: "curate_diagram"
    - from: "global-rules.md Diagram Registry section"
      to: "DocuMind diagrams table"
      via: "Declares DB as canonical source"
      pattern: "single source of truth"
---

<objective>
Rewrite `/figma-curate` to use the `curate_diagram` MCP tool and update `global-rules.md` to declare the DB as the single source of truth for diagrams.

Purpose: `/figma-curate` currently instructs agents to manually edit DIAGRAM-REGISTRY.md and use curl to call DocuMind's REST API. Now that `curate_diagram` (MCP Tool 13) handles all of this in one call, the command should use it. The global-rules.md still implies DIAGRAM-REGISTRY.md is the source of truth — it needs to reflect the v3.0 decision that the DB is authoritative.

Output: Two updated files in RootDispatcher
</objective>

<execution_context>
@/Users/dave/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-mcp-server-write-tools/05-02-SUMMARY.md

<interfaces>
<!-- MCP tool available in daemon/mcp-server.mjs -->

Tool 13: curate_diagram
- Parameters: diagram_name (required string), curated_url (required string — the new FigJam URL from the central board)
- Returns: { success, diagram: { name, curated_url, status: "curated", ... }, propagation: { markdown_files_updated: number, repos_affected: [...] } }
- Handles: DB update, DIAGRAM-REGISTRY.md regeneration, cross-repo markdown URL replacement
- This single tool call replaces the 3-step process of: edit registry file, replace URLs in markdown, call curl to propagate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /figma-curate to use curate_diagram MCP tool</name>
  <files>/Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/figma-curate.md</files>
  <action>
Rewrite the `/figma-curate` slash command so it uses the `curate_diagram` MCP tool instead of manual file editing and curl calls.

Changes to make:

1. **Frontmatter**: Add `mcp__documind__curate_diagram` to allowed-tools. Remove Bash from allowed-tools (no more curl calls needed). Keep Read, Edit, Glob, Grep for potential fallback verification.

2. **Step 1 — Validate**: Replace the current "Read docs/diagrams/DIAGRAM-REGISTRY.md" approach with:
   - Call `get_diagrams` MCP tool filtered by the provided diagram name (or with no filter, then find the entry).
   - Confirm the diagram exists and has status "generated" (not already curated).
   - Show the current Generated URL and ask for confirmation.
   - Fallback: If MCP unavailable, read local DIAGRAM-REGISTRY.md as before.

3. **Step 2 — Curate via MCP**: Replace the old Step 2 (manual registry edit) + Step 3 (manual markdown URL replacement) + Step 4 (curl to DocuMind) with a SINGLE step:
   - Call `curate_diagram` MCP tool with `diagram_name` and `curated_url` parameters.
   - The tool handles everything: DB update, registry regeneration, cross-repo markdown propagation.
   - Report the result: number of markdown files updated, repos affected.

4. **Step 3 — Report**: Simplify the report. The MCP tool response contains propagation details. Print:
   - Diagram name
   - Old URL (from validation step) to New URL
   - Markdown files updated (from tool response)
   - Repos affected (from tool response)
   - Reminder: "You can safely delete the standalone FigJam file now"

5. **Bulk Mode**: Update to use a loop of `curate_diagram` calls instead of the bulk-relink curl endpoint. Remove the curl command entirely.

6. **Notes section**: Replace "Always update the local registry FIRST, then propagate via DocuMind" with "curate_diagram handles the full pipeline: DB update, DIAGRAM-REGISTRY.md regeneration, and cross-repo markdown URL propagation in one call."

Also add `mcp__documind__get_diagrams` to allowed-tools (used in Step 1 for validation).
  </action>
  <verify>
    grep -q "curate_diagram" /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/figma-curate.md && echo "PASS: curate_diagram referenced" || echo "FAIL"
    grep -q "curl" /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/figma-curate.md && echo "FAIL: curl still present" || echo "PASS: no curl calls"
  </verify>
  <done>
    /figma-curate uses curate_diagram MCP tool as the sole curation method. No manual file editing or curl calls remain. Bulk mode uses repeated MCP calls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update global-rules.md to declare DB as single source of truth</name>
  <files>/Users/Shared/htdocs/github/DVWDesign/RootDispatcher/memory/global-rules.md</files>
  <action>
Update the "Diagram Registry" subsection within the "Diagrams and Visualization" section of global-rules.md to reflect the v3.0 decision that the DocuMind `diagrams` table is the single source of truth.

Changes to make:

1. **Diagram Registry subsection** (currently starts with "Each repo maintains a docs/diagrams/DIAGRAM-REGISTRY.md to index all diagram artifacts. This is the **source of truth** for diagram URLs."):
   - Change the opening sentence to: "The DocuMind `diagrams` table is the **single source of truth** for all diagram metadata, URLs, and status. Each repo's `docs/diagrams/DIAGRAM-REGISTRY.md` is a **generated snapshot** — regenerated automatically during daily/weekly scans and after any `register_diagram` or `curate_diagram` MCP tool call. Do not edit DIAGRAM-REGISTRY.md manually."
   - Keep the 7-column format table as reference (agents still need to know the format for reading the snapshot).
   - Keep the field descriptions (Generated URL, Curated URL, Status, Active URL logic).

2. **Diagram Relinking Protocol subsection**:
   - Update step 3 to reference the `curate_diagram` MCP tool instead of manual DIAGRAM-REGISTRY.md editing.
   - Replace the curl-based "DocuMind endpoints" block with MCP tool references:
     - `curate_diagram` MCP tool — sets curated URL, propagates to DB + markdown + regenerates snapshot
     - `register_diagram` MCP tool — registers new diagrams from .mmd files
     - `get_diagrams` MCP tool — queries diagram status and metadata
   - Remove the curl endpoint examples (`POST /diagrams/relink`, `GET /diagrams/pending-relinks`, `POST /diagrams/sync-registry`) — agents should use MCP tools, not REST endpoints.

3. **Update the "Last updated" date** at the top of the file to 2026-03-22.
  </action>
  <verify>
    grep -q "single source of truth" /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/memory/global-rules.md && echo "PASS: single source of truth declared" || echo "FAIL"
    grep -q "generated snapshot" /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/memory/global-rules.md && echo "PASS: snapshot language present" || echo "FAIL"
    grep -q "curate_diagram" /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/memory/global-rules.md && echo "PASS: MCP tool referenced" || echo "FAIL"
  </verify>
  <done>
    global-rules.md declares the DocuMind diagrams table as single source of truth. DIAGRAM-REGISTRY.md described as generated snapshot. MCP tools referenced instead of curl endpoints.
  </done>
</task>

</tasks>

<verification>
1. figma-curate.md references curate_diagram MCP tool and contains no curl commands
2. global-rules.md contains "single source of truth" referring to the DB, not the file
3. global-rules.md contains "generated snapshot" referring to DIAGRAM-REGISTRY.md
4. Relinking protocol references MCP tools instead of REST endpoints
</verification>

<success_criteria>
- /figma-curate uses curate_diagram MCP tool as sole curation method
- global-rules.md declares diagrams table as single source of truth
- DIAGRAM-REGISTRY.md is described as a generated snapshot everywhere
- No curl calls remain in figma-curate command
</success_criteria>

<output>
After completion, create `.planning/phases/08-slash-command-updates/08-02-SUMMARY.md`
</output>
