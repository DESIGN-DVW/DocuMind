---

phase: 08-slash-command-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:

  - /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/diagram-registry.md

  - /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/figma-diagram.md

autonomous: true
requirements: [SLSH-01, SLSH-02]

must_haves:
  truths:

    - "/diagram-registry retrieves diagram data via get_diagrams MCP tool, not local file lookup"

    - "/figma-diagram Step 4 registers new diagrams via register_diagram MCP tool, not by editing DIAGRAM-REGISTRY.md"

  artifacts:

    - path: "/Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/diagram-registry.md"

      provides: "Rewritten slash command using get_diagrams MCP tool"
      contains: "get_diagrams"

    - path: "/Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/figma-diagram.md"

      provides: "Updated slash command using register_diagram MCP tool in Step 4"
      contains: "register_diagram"
  key_links:

    - from: "diagram-registry.md"

      to: "get_diagrams MCP tool (Tool 6)"
      via: "MCP tool call replaces local file read"
      pattern: "get_diagrams"

    - from: "figma-diagram.md"

      to: "register_diagram MCP tool (Tool 14)"
      via: "MCP tool call replaces DIAGRAM-REGISTRY.md file edit"
      pattern: "register_diagram"

---

<objective>
Rewrite `/diagram-registry` and `/figma-diagram` slash commands to use MCP tools as their backend instead of direct file reads and manual registry edits.

Purpose: These slash commands currently read DIAGRAM-REGISTRY.md directly and edit it manually. Now that DocuMind's MCP server has `get_diagrams` (Tool 6) and `register_diagram` (Tool 14), the commands should use those tools as the authoritative data source.

Output: Two updated slash command files in RootDispatcher/commands/
</objective>

<execution_context>
@/Users/dave/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-diagram-registry-completion/07-01-SUMMARY.md
@.planning/phases/06-mcp-intelligence-tools/06-01-SUMMARY.md

<interfaces>
<!-- MCP tools available in daemon/mcp-server.mjs -->

Tool 6: get_diagrams

- Parameters: repository (optional string), status (optional: "generated"|"curated"|"stale"), include_stale (optional boolean, default false)

- Returns: { total, diagrams: [{ id, name, mmd_path, png_path, generated_url, curated_url, diagram_type, status, source_hash, updated_at, repository }] }

Tool 14: register_diagram

- Parameters: mmd_path (required string — absolute path to .mmd file)

- Returns: { success, action: "inserted"|"updated"|"unchanged", diagram: { name, mmd_path, diagram_type, source_hash, ... } }

- Auto-detects diagram_type from .mmd content

- Regenerates DIAGRAM-REGISTRY.md snapshot after registration

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /diagram-registry to use get_diagrams MCP tool</name>
  <files>/Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/diagram-registry.md</files>
  <action>
Rewrite the `/diagram-registry` slash command so it retrieves all diagram data via the `get_diagrams` MCP tool instead of reading `docs/diagrams/DIAGRAM-REGISTRY.md` from the filesystem.

Changes to make:

1. **Frontmatter**: Add `mcp__documind__get_diagrams` to allowed-tools list (keep existing Bash, Read, etc. for file cross-checks).

2. **Step 1 — Query DocuMind**: Replace "Locate Registry" with a call to the `get_diagrams` MCP tool. Call it three times or once with no filter:

   - `get_diagrams` with no params (all diagrams)

   - The response includes status per diagram, so derive counts from the result array.

   If DocuMind MCP is not available (tool not found), fall back to reading local `docs/diagrams/DIAGRAM-REGISTRY.md` — but note this is the fallback, not primary path.

3. **Step 2 — Parse and Display**: Keep the same display format (Status Counts, Pending Actions, Full Table) but derive all data from the MCP response `diagrams` array instead of parsing a markdown table. Status counts come from filtering the array by `status` field. Pending actions come from entries where `status === "generated"` (needs curation), `status === "stale"` (needs regeneration), or `curated_url` is null.

4. **Step 3 — Cross-check Files**: Keep this step — it verifies `.mmd` and `.png` files exist on disk. Use the `mmd_path` and `png_path` fields from the MCP response to check file existence.

5. **Step 4**: Remove the "DocuMind Integration (if available)" section entirely — DocuMind IS now the primary data source via MCP, not an optional enhancement. Remove the curl commands.

6. **Step 5 — Suggest Actions**: Keep as-is (suggest /figma-diagram, /figma-curate).

7. **Notes section**: Update to state: "The DocuMind `diagrams` table is the single source of truth. DIAGRAM-REGISTRY.md is a generated snapshot — do not edit it manually." Remove the line saying "DIAGRAM-REGISTRY.md is the source of truth".

  </action>
  <verify>
    grep -q "get_diagrams" /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/diagram-registry.md && echo "PASS: get_diagrams referenced" || echo "FAIL"
    grep -qv "DIAGRAM-REGISTRY.md is the source of truth" /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/diagram-registry.md && echo "PASS: old source-of-truth removed" || echo "FAIL"
  </verify>
  <done>
    /diagram-registry command uses get_diagrams MCP tool as primary data source, with local file read as fallback only. Notes declare DB as source of truth.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite /figma-diagram Step 4 to use register_diagram MCP tool</name>
  <files>/Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/figma-diagram.md</files>
  <action>
Update the `/figma-diagram` slash command so Step 4 uses the `register_diagram` MCP tool instead of manually editing DIAGRAM-REGISTRY.md.

Changes to make:

1. **Frontmatter**: Add `mcp__documind__register_diagram` to allowed-tools list.

2. **Step 4 — Update Diagram Registry**: Replace the current instructions ("Add or update the entry in docs/diagrams/DIAGRAM-REGISTRY.md") with:

   - Call `register_diagram` MCP tool with the `mmd_path` parameter set to the absolute path of the `.mmd` file created in Step 1.

   - The tool auto-detects diagram type, computes source hash, inserts/updates the DB, and regenerates DIAGRAM-REGISTRY.md automatically.

   - Report the tool response: action taken ("inserted", "updated", or "unchanged") and the detected diagram_type.

   - If the MCP tool is unavailable (tool not found), fall back to manually editing DIAGRAM-REGISTRY.md as currently described — but note this is a fallback.

   - Remove the markdown code block showing the 7-column registry format (the MCP tool handles this).

3. **Step 5 — Insert FigJam Link**: Keep as-is (this step inserts the link into markdown, which is separate from registry management).

4. **Step 6 — Report**: Add "Registry action: {inserted/updated/unchanged}" to the summary output.

5. **Notes section**: Add note: "register_diagram auto-detects diagram type and regenerates DIAGRAM-REGISTRY.md — no manual registry editing needed."

  </action>
  <verify>
    grep -q "register_diagram" /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/figma-diagram.md && echo "PASS: register_diagram referenced" || echo "FAIL"
    grep -q "mcp__documind__register_diagram" /Users/Shared/htdocs/github/DVWDesign/RootDispatcher/commands/figma-diagram.md && echo "PASS: tool in allowed-tools" || echo "FAIL"
  </verify>
  <done>
    /figma-diagram Step 4 uses register_diagram MCP tool as primary method to register new diagrams. Manual DIAGRAM-REGISTRY.md editing is fallback only.
  </done>
</task>

</tasks>

<verification>

1. Both slash command files reference their respective MCP tools

2. Neither file treats DIAGRAM-REGISTRY.md as the primary source of truth

3. Both files have MCP tools listed in their allowed-tools frontmatter

4. Fallback paths exist for when MCP is unavailable

</verification>

<success_criteria>

- /diagram-registry uses get_diagrams MCP tool for data retrieval

- /figma-diagram Step 4 uses register_diagram MCP tool for diagram registration

- DIAGRAM-REGISTRY.md is described as a generated snapshot, not source of truth

- Both commands remain functional with graceful fallback if MCP unavailable

</success_criteria>

<output>
After completion, create `.planning/phases/08-slash-command-updates/08-01-SUMMARY.md`
</output>
